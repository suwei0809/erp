<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sw.erp.backstage.manage.mapper.RecruitMapper">

    <select id="getRecruit" resultType="com.sw.erp.backstage.manage.model.Recruit">
        SELECT
        R_ID AS id,
        R_POSITION AS job,
        R_PLACE AS place,
        R_SALARY AS salary,
        R_REQUIRE AS require,
        R_CONTACT AS contact,
        R_PNUMBER AS pnumber,
        R_EDUCATION AS education,
        R_EXPERIENCE AS experience,
        R_DATE AS beginDate
        FROM
        T_RECRUIT
    </select>
</mapper>